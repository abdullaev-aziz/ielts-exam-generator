# NATS server config for AI IELTS project
# Start with: nats-server -c nats.conf

jetstream {
    store_dir: "/tmp/nats-ielts"
    max_memory_store: 256mb
    max_file_store: 1gb
}

# Connection settings
max_connections: 100
ping_interval: "30s"
ping_max: 3
write_deadline: "10s"
